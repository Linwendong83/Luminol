From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Codex <codex@openai.com>
Date: Tue, 22 Sep 2026 10:00:00 +0800
Subject: [PATCH] Restore custom portal respawn spectator and sleep contracts


diff --git a/io/papermc/paper/threadedregions/TeleportUtils.java b/io/papermc/paper/threadedregions/TeleportUtils.java
index 335e9775e0d9a61f0e9641aba691a3bdf3ac7f2b..c0acddb097d61c9abc2950345957216ea5635639 100644
--- a/io/papermc/paper/threadedregions/TeleportUtils.java
+++ b/io/papermc/paper/threadedregions/TeleportUtils.java
@@ -40,6 +40,21 @@ public final class TeleportUtils {
                             }
                             return;
                         }
+                        // Lophine start - cancellable spectator teleport, on the observer's region
+                        if (cause == PlayerTeleportEvent.TeleportCause.SPECTATE
+                                && realFrom instanceof net.minecraft.server.level.ServerPlayer serverPlayer
+                                && serverPlayer.isSpectator()) {
+                            final me.earthme.luminol.api.entity.player.PlayerSpectateTeleportEvent event =
+                                    new me.earthme.luminol.api.entity.player.PlayerSpectateTeleportEvent(
+                                            serverPlayer.getBukkitEntity(), to.getBukkitEntity(), loc);
+                            if (!event.callEvent()) {
+                                if (onComplete != null) {
+                                    onComplete.accept(null);
+                                }
+                                return;
+                            }
+                        }
+                        // Lophine end - cancellable spectator teleport
                         (useFromRootVehicle ? realFrom.getRootVehicle() : realFrom).teleportAsync(
                             ((CraftWorld)loc.getWorld()).getHandle(), pos, yaw, pitch, null, // Camellia - Fix teleport missing original rotation and velocity
                             cause, teleportFlags, onComplete
@@ -79,4 +94,4 @@ public final class TeleportUtils {
     }
 
     private TeleportUtils() {}
-}
\ No newline at end of file
+}
diff --git a/net/minecraft/server/level/ServerPlayer.java b/net/minecraft/server/level/ServerPlayer.java
index 28ce624e9952e9d45930d4550ef80d1e788f5916..fe89ff3eb903123c67009ed37c1dfb0b9dc9031a 100644
--- a/net/minecraft/server/level/ServerPlayer.java
+++ b/net/minecraft/server/level/ServerPlayer.java
@@ -1810,6 +1810,10 @@ public class ServerPlayer extends Player implements ca.spottedleaf.moonrise.patc
         ca.spottedleaf.concurrentutil.completable.CallbackCompletable<org.bukkit.Location> spawnPosComplete =
             new ca.spottedleaf.concurrentutil.completable.CallbackCompletable<>();
         boolean[] usedRespawnAnchor = new boolean[1];
+        // Lophine - carry the resolved spawn flags through async placement
+        boolean[] postRespawnBedSpawn = new boolean[1];
+        boolean[] postRespawnAnchorSpawn = new boolean[1];
+        boolean[] postRespawnMissingBlock = new boolean[1];
 
         // set up post spawn location logic
         spawnPosComplete.addWaiter((spawnLoc, throwable) -> {
@@ -1847,7 +1851,10 @@ public class ServerPlayer extends Player implements ca.spottedleaf.moonrise.patc
                         finalRespawnCompleteCallback.accept(ServerPlayer.this); // Luminol - Level hot unload apis
                     }
                     // Luminol - Add missing teleportation apis
-                    new me.earthme.luminol.api.entity.player.PostPlayerRespawnEvent(ServerPlayer.this.getBukkitEntity()).callEvent();
+                    new me.earthme.luminol.api.entity.player.PostPlayerRespawnEvent(
+                            ServerPlayer.this.getBukkitEntity(), spawnLoc,
+                            postRespawnBedSpawn[0], postRespawnAnchorSpawn[0], postRespawnMissingBlock[0], reason
+                    ).callEvent();
                     // Luminol end
                 }
             );
@@ -1868,6 +1875,7 @@ public class ServerPlayer extends Player implements ca.spottedleaf.moonrise.patc
                         respawnWorld, respawnConfig, !alive
                     ).orElse(null);
                     if (spawnPos == null) {
+                        postRespawnMissingBlock[0] = true; // Lophine - async respawn event flags
                         // no spawn
                         ServerPlayer.this.connection.send(
                             new ClientboundGameEventPacket(ClientboundGameEventPacket.NO_RESPAWN_BLOCK_AVAILABLE, 0.0F)
@@ -1883,6 +1891,8 @@ public class ServerPlayer extends Player implements ca.spottedleaf.moonrise.patc
                     boolean isRespawnAnchor = respawnWorld.getBlockState(respawnConfig.respawnData.pos()).is(net.minecraft.world.level.block.Blocks.RESPAWN_ANCHOR);
                     boolean isBed = respawnWorld.getBlockState(respawnConfig.respawnData.pos()).is(net.minecraft.tags.BlockTags.BEDS);
                     usedRespawnAnchor[0] = !alive && isRespawnAnchor;
+                    postRespawnBedSpawn[0] = spawnPos.isBedSpawn(); // Lophine - use resolved flags, not just the stored block
+                    postRespawnAnchorSpawn[0] = spawnPos.isAnchorSpawn();
 
                     // finished now, pass the location on
                     spawnPosComplete.complete(
diff --git a/net/minecraft/server/players/SleepStatus.java b/net/minecraft/server/players/SleepStatus.java
index 61870e387c93a5f0e62180372438b7011a294cd6..68b56a2ff19f32f66babc73dc8406905cb1f29e4 100644
--- a/net/minecraft/server/players/SleepStatus.java
+++ b/net/minecraft/server/players/SleepStatus.java
@@ -3,7 +3,6 @@ package net.minecraft.server.players;
 import java.util.List;
 import net.minecraft.server.level.ServerPlayer;
 import net.minecraft.util.Mth;
-import net.minecraft.world.entity.player.Player;
 
 public class SleepStatus {
     private int activePlayers;
@@ -15,12 +14,16 @@ public class SleepStatus {
 
     public boolean areEnoughDeepSleeping(final int sleepPercentageNeeded, final List<ServerPlayer> players) {
         // CraftBukkit start
-        int deepSleepers = (int)players.stream().filter(player -> player.isSleepingLongEnough() || player.fauxSleeping).count();
-        boolean anyDeepSleep = players.stream().anyMatch(Player::isSleepingLongEnough);
+        int deepSleepers = (int)players.stream().filter(player -> isSleepActive(player) && (player.isSleepingLongEnough() || player.fauxSleeping)).count();
+        boolean anyDeepSleep = players.stream().anyMatch(player -> isSleepActive(player) && player.isSleepingLongEnough());
         return anyDeepSleep && deepSleepers >= this.sleepersNeeded(sleepPercentageNeeded);
         // CraftBukkit end
     }
 
+    private static boolean isSleepActive(final ServerPlayer player) {
+        return !player.isSpectator() && !player.getBukkitEntity().isSleepingIgnored();
+    }
+
     public int sleepersNeeded(final int sleepPercentageNeeded) {
         return Math.max(1, Mth.ceil(this.activePlayers * sleepPercentageNeeded / 100.0F));
     }
@@ -41,17 +44,18 @@ public class SleepStatus {
         boolean anySleep = false; // CraftBukkit
 
         for (ServerPlayer player : players) {
-            if (!player.isSpectator()) {
-                this.activePlayers++;
-                if (player.isSleeping() || player.fauxSleeping) { // CraftBukkit
-                    this.sleepingPlayers++;
-                }
-                // CraftBukkit start
-                if (player.isSleeping()) {
-                    anySleep = true;
-                }
-                // CraftBukkit end
+            if (!isSleepActive(player)) {
+                continue;
+            }
+            this.activePlayers++;
+            if (player.isSleeping() || player.fauxSleeping) { // CraftBukkit
+                this.sleepingPlayers++;
+            }
+            // CraftBukkit start
+            if (player.isSleeping()) {
+                anySleep = true;
             }
+            // CraftBukkit end
         }
 
         return anySleep && (oldSleepingPlayers > 0 || this.sleepingPlayers > 0) && (oldActivePlayers != this.activePlayers || oldSleepingPlayers != this.sleepingPlayers); // CraftBukkit
diff --git a/net/minecraft/world/entity/Entity.java b/net/minecraft/world/entity/Entity.java
index 8d98fdb775703157b2cf6944d68e39b823993067..a9dd987235d64e7d96588dbf5bf3268773204515 100644
--- a/net/minecraft/world/entity/Entity.java
+++ b/net/minecraft/world/entity/Entity.java
@@ -4756,7 +4756,8 @@ public abstract class Entity
                                     destination, finalPos, this.getDeltaMovement(), Direction.WEST.toYRot(), 0.0f, // Kaiiju - Vanilla end teleportation
                                     false, false,
                                     /*Relative.union(Relative.DELTA, Set.of(Relative.X_ROT))*/Set.of(), // Kaiiju - Vanilla end teleportation
-                                    TeleportTransition.PLAY_PORTAL_SOUND.then(TeleportTransition.PLACE_PORTAL_TICKET),
+                                    TeleportTransition.PLAY_PORTAL_SOUND.then(TeleportTransition.PLACE_PORTAL_TICKET)
+                                        .then(entity -> net.minecraft.world.level.levelgen.feature.EndPlatformFeature.createEndPlatform(destination, targetPos.below(), true, null)), // Lophine - before completion event and unload reference release
                                     org.bukkit.event.player.PlayerTeleportEvent.TeleportCause.END_PORTAL,
                                     TeleportTransition.PassengerTeleportationMode.POSITION_RIDER
                                 )
@@ -4981,10 +4982,17 @@ public abstract class Entity
             return false;
         }
         // Luminol start - Add missing teleportation events
+        final org.bukkit.Location portalFrom = io.papermc.paper.util.MCUtil.toLocation(this.level, portalPos);
+        final org.bukkit.PortalType bukkitPortalType = type == PortalType.END ? org.bukkit.PortalType.ENDER : org.bukkit.PortalType.NETHER;
+        final org.bukkit.event.player.PlayerTeleportEvent.TeleportCause portalCause = type == PortalType.END
+                ? org.bukkit.event.player.PlayerTeleportEvent.TeleportCause.END_PORTAL
+                : org.bukkit.event.player.PlayerTeleportEvent.TeleportCause.NETHER_PORTAL;
         if (!new me.earthme.luminol.api.entity.PreEntityPortalEvent(
                 this.getBukkitEntity(),
-                io.papermc.paper.util.MCUtil.toLocation(this.level, portalPos),
-                destination.getWorld()
+                portalFrom,
+                destination.getWorld(),
+                bukkitPortalType,
+                portalCause
         ).callEvent()) {
             return false;
         }
@@ -5015,10 +5023,17 @@ public abstract class Entity
         }
         // Lophine end - destination-driven portal backpressure
 
-        // Kaiiju start - sync end platform spawning & entity teleportation
-        final java.util.function.Consumer<Entity> tpComplete = type == PortalType.END && destination.getTypeKey() == net.minecraft.world.level.dimension.LevelStem.END ?
-                e -> {net.minecraft.world.level.levelgen.feature.EndPlatformFeature.createEndPlatform(destination, ServerLevel.END_SPAWN_POINT.below(), true, null); if (teleportComplete != null) {teleportComplete.accept(e);}} : teleportComplete;
-        // Kaiiju end
+        // Lophine start - notify only after placement, preserving the caller's callback
+        final java.util.function.Consumer<Entity> portalComplete = teleported -> {
+            new me.earthme.luminol.api.entity.PostEntityPortalEvent(
+                    teleported.getBukkitEntity(), portalFrom, teleported.getBukkitEntity().getLocation(),
+                    bukkitPortalType, portalCause
+            ).callEvent();
+            if (teleportComplete != null) {
+                teleportComplete.accept(teleported);
+            }
+        };
+        // Lophine end - portal completion event
         Vec3 initialPosition = this.position();
         ChunkPos initialPositionChunk = new ChunkPos(
             ca.spottedleaf.moonrise.common.util.CoordinateUtils.getChunkX(initialPosition),
@@ -5093,14 +5108,7 @@ public abstract class Entity
                     destination.levelUnloadStateLock.releaseRead();
                     // Luminol end
 
-                    // Kaiiju start - vanilla end teleportation
-                    /*if (teleportComplete != null) {
-                        teleportComplete.accept(teleported);
-                    }*/
-                    if (tpComplete != null){
-                        tpComplete.accept(teleported);
-                    }
-                    // Kaiiju end
+                    portalComplete.accept(teleported); // Lophine - successful placement only
                 }
             );
         });
